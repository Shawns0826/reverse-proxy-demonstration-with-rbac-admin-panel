# reference-client

Android TV client that calls the **upstream API directly** on port **5001**.

Default `api_base_url`: `http://10.0.2.2:5001` (official emulator → host).

Import this folder in Android Studio. Start backend with `docker compose up --build` first.
