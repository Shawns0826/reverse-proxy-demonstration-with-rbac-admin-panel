# upstream-service

Mock streaming provider API (`/api/auths/local`, `/api/users/valide`).

- **Port:** 5001
- **Demo user:** `username1234` / `password1234`

See the [repository README](../README.md) for `docker compose up`.
